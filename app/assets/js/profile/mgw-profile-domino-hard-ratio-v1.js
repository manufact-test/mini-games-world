/* MVP-19.9 Domino Profile geometry compatibility shim.
   Store/Profile shared CSS owns the 8:5 geometry. No imperative sizing, retries or second geometry owner. */
export function initProfileDominoHardRatio(){}
