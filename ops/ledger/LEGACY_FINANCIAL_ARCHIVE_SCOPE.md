# Scope

Included: inactive archive schema, exact snapshots, hashes, source positions, status normalization, SQLite/MySQL/MariaDB checks.

Excluded: JSON import, production execution, runtime reads/writes, payment/shop shutdown, new cosmetic store, `/app`, `/site`.
