# Archive table ownership

`mgw_legacy_payments`, `mgw_legacy_shop_orders` and `mgw_legacy_financial_transactions` are server-only history tables. They are not active economy tables and are never read by gameplay, settlement, payment creation or the shop.
