name: MVP-19.9 Domino Store/Profile premium v15 proportions

on:
  pull_request:
    branches: [agent/mvp-13-2-staging]
    paths:
      - 'app/assets/js/screens/store-screen.js'
      - 'app/assets/css/games/domino/store-effects-scene-v9.css'
      - 'app/assets/css/games/domino/store-effects-live-parity-v41.css'
      - 'app/assets/js/screens/store-screen-domino-store-v1.js'
      - 'app/assets/js/screens/store-screen-domino-effects-v9.js'
      - 'app/assets/js/screens/store-screen-checkers-board-source-wrapper.js'
      - 'app/assets/js/profile/mgw-profile-domino-parity.js'
      - 'app/runtime/client/version-manifest.php'
      - 'bot/helpers/WebAppLaunchUrl.php'
      - 'bot/tests/Mvp19_9DominoStoreProfileTest.php'
      - 'bot/tests/Mvp19_9DominoPreviewReadabilityContractTest.mjs'
      - '.github/workflows/mvp19-9-domino-store-effects-v8.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: mgw-mvp19-9-domino-premium-v15-${{ github.ref }}
  cancel-in-progress: true

jobs:
  domino-store-profile-v15:
    runs-on: ubuntu-24.04
    timeout-minutes: 18
    steps:
      - name: Checkout exact candidate
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: pdo_sqlite, mbstring
          coverage: none

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Syntax and v15 Domino contracts
        run: |
          set -euo pipefail
          php -l app/runtime/client/version-manifest.php
          php -l bot/helpers/WebAppLaunchUrl.php
          php -l bot/tests/Mvp19_9DominoStoreProfileTest.php
          node --check app/assets/js/screens/store-screen.js
          node --check app/assets/js/screens/store-screen-domino-store-v1.js
          node --check app/assets/js/screens/store-screen-domino-effects-v9.js
          node --check app/assets/js/screens/store-screen-checkers-board-source-wrapper.js
          node --check app/assets/js/profile/mgw-profile-domino-parity.js
          node --check bot/tests/Mvp19_9DominoPreviewReadabilityContractTest.mjs
          php bot/tests/Mvp19_9DominoStoreProfileTest.php
          node bot/tests/Mvp19_9DominoPreviewReadabilityContractTest.mjs

      - name: Guard v15 scene and cache ownership
        shell: bash
        run: |
          set -euo pipefail
          base='app/assets/js/screens/store-screen.js'
          store='app/assets/js/screens/store-screen-domino-store-v1.js'
          effects='app/assets/css/games/domino/store-effects-scene-v9.css'
          live_parity='app/assets/css/games/domino/store-effects-live-parity-v41.css'
          wrapper='app/assets/js/screens/store-screen-checkers-board-source-wrapper.js'
          profile='app/assets/js/profile/mgw-profile-domino-parity.js'
          manifest='app/runtime/client/version-manifest.php'

          grep -F "store-screen-domino-store-v1.js?v=8&mvp19_9=domino-native-render-v8" "$base"
          grep -F "'precision-drop':'Яркий акцент в момент точного хода'" "$base"
          grep -F 'native:v10:live-v41' "$store"
          grep -F 'data-mgw-domino-fx-v14' "$store"
          grep -F 'sceneReady' "$store"
          grep -F 'data-mgw-domino-preview-live-v41' "$store"
          grep -F 'data-mgw-domino-preview-particles="8"' "$store"
          grep -F 'data-mgw-domino-preview-particles="12"' "$store"
          grep -F 'data-mgw-domino-preview-particles="6"' "$store"
          grep -F 'ensureEffectStyles();' "$store"
          grep -F 'domino-premium-effects-v15-proportions' "$store"

          grep -F '[data-cosmetic-layer="effect"]' "$effects"
          grep -F 'aspect-ratio:8 / 5!important' "$effects"
          grep -F 'inset:0!important' "$effects"
          grep -F 'height:100%!important' "$effects"
          grep -F 'width:29.5%!important' "$effects"
          grep -F 'height:23.6%!important' "$effects"
          grep -F 'width:31%!important' "$effects"
          grep -F 'height:24.8%!important' "$effects"
          grep -F 'width:13.5%!important' "$effects"
          grep -F 'height:43.2%!important' "$effects"
          grep -F 'mgw-domino-v15-precision-flight' "$effects"
          grep -F 'mgw-domino-v15-stock-flight' "$effects"
          grep -F 'mgw-domino-v15-cascade-tile' "$effects"
          grep -F 'mgw-domino-v41-precision-shard' "$live_parity"
          grep -F 'mgw-domino-v41-stock-line' "$live_parity"
          grep -F 'height:.62px!important' "$live_parity"
          grep -F 'mgw-domino-v41-finale-sweep' "$live_parity"
          ! grep -Fq 'mgw-domino-v12-' "$effects"
          ! grep -Fq 'repeating-conic-gradient' "$effects"
          ! grep -Fq 'mix-blend-mode:screen' "$effects"
          ! grep -Fq 'transform-style:preserve-3d' "$effects"

          grep -F "store-screen-domino-store-v1.js?v=10&mvp19_9=domino-live-preview-v41" "$wrapper"
          grep -F "store-screen-domino-effects-v9.js?v=8&mvp19_9=domino-live-preview-v41" "$wrapper"
          grep -F "store-screen-domino-store-v1.js?v=10&mvp19_9=domino-live-preview-v41" "$profile"
          grep -F 'store-effects-scene-v9.css?v=7&mvp19_9=domino-premium-effects-v15-proportions' "$profile"
          grep -F 'store-effects-live-parity-v41.css?v=1&mvp19_9=domino-live-preview-v41' "$profile"
          grep -F 'domino_effects=live-v41' "$manifest"
          grep -F "store-screen-domino-store-v1.js?v=8&mvp19_9=domino-native-render-v8' => './assets/js/screens/store-screen-domino-store-v1.js?v=10&mvp19_9=domino-live-preview-v41" "$manifest"
          grep -F 'store-screen.js?v=48&intent_base=1' "$manifest"
          php -r '$s=file_get_contents("bot/helpers/WebAppLaunchUrl.php"); if(!preg_match("~/app/v110\\.php\\?v=(\\d+)~", $s, $m) || (int)$m[1] < 1170) exit(1);'

      - name: Guard accepted static Domino and frozen live gameplay
        shell: bash
        run: |
          set -euo pipefail
          grep -F 'height:100%!important' app/assets/css/games/domino/store-card-fill-live-pips-v5.css
          grep -F 'width:3px!important' app/assets/css/games/domino/store-card-fill-live-pips-v5.css
          grep -F 'theme-walnut .mgw-domino-preview-table' app/assets/css/games/domino/store-cosmetics-v1.css
          if [ '${{ github.event_name }}' = 'pull_request' ]; then
            base='${{ github.event.pull_request.base.sha }}'; head='${{ github.event.pull_request.head.sha }}'
            git diff --exit-code "$base" "$head" -- \
              app/assets/css/games/domino/store-cosmetics-v1.css \
              app/assets/css/games/domino/store-card-fill-live-pips-v5.css \
              app/assets/js/games/domino/entry.js \
              app/assets/js/games/domino/meta.js \
              app/assets/js/games/domino/renderer.js \
              app/assets/js/games/domino/rules.js \
              app/assets/js/games/domino/chain-layout.js \
              app/assets/css/games/domino/game.css \
              app/assets/css/games/domino/rules.css \
              app/assets/js/games/go \
              app/assets/js/games/reversi \
              app/assets/js/games/chess \
              app/assets/js/games/checkers \
              app/assets/js/checkers-cosmetics \
              bot/catalog/ProductInventoryService.php \
              bot/database/migrations
          fi
          git diff --check
