name: Tech apply D2 terminal dedup clean

on:
  pull_request:
    branches:
      - agent/mvp14-d2-tech-base

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'agent/mvp14-d2-tech-run-v2'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout patch tool
        uses: actions/checkout@v4
        with:
          ref: agent/mvp14-d2-tech-base
          path: tech
          fetch-depth: 1

      - name: Checkout exact feature branch
        uses: actions/checkout@v4
        with:
          ref: agent/mvp14-d2-terminal-dedup-self-cancel-home
          path: target
          fetch-depth: 1
          persist-credentials: true

      - name: Apply focused product and acceptance changes
        working-directory: target
        run: python3 ../tech/.github/scripts/run_d2_terminal_dedup.py

      - name: Commit clean focused branch
        working-directory: target
        shell: bash
        run: |
          set -Eeuo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git diff --cached --quiet && { echo 'No changes produced'; exit 1; }
          git commit -m "MVP-14 D2: remove duplicate terminal hints and return self-cancel home"
          git push origin HEAD:agent/mvp14-d2-terminal-dedup-self-cancel-home
