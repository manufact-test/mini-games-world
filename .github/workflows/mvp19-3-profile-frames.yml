name: MVP-19.3 Profile Frames

on:
  pull_request:
    branches: [agent/mvp-13-2-staging]
    paths:
      - 'bot/cosmetic-store.php'
      - 'bot/helpers/response.php'
      - 'app/runtime/client/version-manifest.php'
      - 'app/assets/js/profile/**'
      - 'bot/tests/Mvp19_3ProfileFramesTest.php'
      - '.github/workflows/mvp19-3-profile-frames.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frames:
    name: Preserve accepted Profile frames
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: pdo_sqlite, mbstring
          coverage: none
      - name: Run frame contract
        run: php bot/tests/Mvp19_3ProfileFramesTest.php
      - name: Guard accepted frame owners
        if: ${{ github.event_name == 'pull_request' }}
        shell: bash
        run: |
          set -euo pipefail
          base='${{ github.event.pull_request.base.sha }}'; head='${{ github.event.pull_request.head.sha }}'
          git diff --exit-code "$base" "$head" -- app/assets/js/profile/mgw-profile-frames.js bot/tests/Mvp19_3ProfileFramesTest.php
          git diff --check
