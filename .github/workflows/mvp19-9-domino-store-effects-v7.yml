name: MVP-19.9 Domino Store effects v7 (historical)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-note:
    runs-on: ubuntu-24.04
    steps:
      - name: Historical v7 contract retired
        run: |
          echo 'Domino Store effect v7 was superseded by the deterministic v9 Store/Profile repair.'
          echo 'This workflow is retained only as historical evidence and must not gate current PRs.'
